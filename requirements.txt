# --- Core ---
python-dotenv==1.0.1

# --- Database / vector store ---
psycopg2-binary==2.9.9
pgvector==0.3.6

# --- Embeddings (fully local, free, no API key needed) ---
sentence-transformers==3.0.1

# --- LLM providers (free tiers) ---
google-generativeai==0.8.3
groq==1.7.0

# --- Document loading ---
pypdf==5.0.1

# --- Agent orchestration (Phase 2) ---
langgraph==1.2.11

# --- Observability (Phase 3) ---
# NOTE: ragas is deliberately NOT included -- ragas==0.4.3 requires
# langchain_community/langchain_openai versions that conflict with
# langgraph==1.2.11's langchain-core>=1.4 requirement. Phase 3's eval
# harness (src/eval.py) reimplements the same metrics as direct
# LLM-as-judge calls instead, using the LLM provider you already have.
langfuse==4.15.3

# --- Cost & latency optimization (Phase 4) ---
redis==6.4.0
ollama==0.6.2

# --- API serving layer (Phase 5) ---
fastapi==0.141.1
uvicorn[standard]==0.53.0
python-multipart==0.0.32

# --- Misc ---
numpy<2.0
