# Not installed in the Docker image -- these are for local development and
# CI only. Install with: pip install -r requirements.txt -r requirements-dev.txt

pytest==9.1.1
pytest-mock==3.15.1
ruff==0.16.8
